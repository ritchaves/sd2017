package org.komparator.mediator.domain;

import pt.ulisboa.tecnico.sdis.ws.uddi.UDDINaming;

//import org.komparator.mediator.ws.cli.MediatorClient;

public class LifeProof extends Thread { //TimerTask?
	
	public void run() {
		/*if primario - como??!??!?!? {
			MediatorClient secondary = new MediatorClient("http://localhost:8072/mediator-ws/endpoint");
			//TODO De 5 em 5 segundos (valor configurável) chama o método imAlive() no servidor através do cliente
			secondary.imAlive();
		else{
			//verificar o quão antigo é o ultimo imalive e se ultrapassar um dado intervalo de tempo
			 uddiNaming = new UDDINaming(uddiURL);
			 uddiNaming.rebind(wsName, secondary.url);
		}	
			*/
	}

}
